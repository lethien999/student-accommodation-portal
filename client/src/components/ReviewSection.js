import React from 'react';

const ReviewSection = ({ accommodationId }) => {
    return (
        <div className="mt-8 p-6 bg-white rounded-lg shadow-sm border border-gray-100">
            <h3 className="text-xl font-semibold mb-4 text-gray-800">Đánh giá</h3>
            <p className="text-gray-500 italic">Chức năng đánh giá đang được phát triển.</p>
        </div>
    );
};

export default ReviewSection;
