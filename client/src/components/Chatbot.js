import React from 'react';

const Chatbot = () => {
    return (
        <div className="fixed bottom-4 right-4 bg-indigo-600 text-white p-3 rounded-full shadow-lg cursor-not-allowed opacity-50">
            Chatbot (Coming Soon)
        </div>
    );
};

export default Chatbot;
